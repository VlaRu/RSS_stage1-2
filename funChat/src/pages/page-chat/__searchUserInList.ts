/*   const listUsers = document.querySelector('.users-container') as HTMLElement;

  if (listUsers) {
      listUsers.addEventListener('click', (event) => {
          const target = event.target as HTMLElement;

          if (target.tagName === 'LI') {
              console.log(target.textContent);
          }
      });
  } else {
      console.log('Element with class .users-container not found');
  }
 */



